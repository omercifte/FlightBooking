@model FlightBooking.Dtos.AgentDtos.AgentPromptRequestDto

@{
    ViewData["Title"] = "YZ Seyahat Asistanı";
    Layout = null;
}
<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>YZ Seyahat Asistanı · FlightBooking</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Syne:wght@400;600;700;800&family=DM+Sans:ital,opsz,wght@0,9..40,300;0,9..40,400;0,9..40,500;0,9..40,600;1,9..40,300&display=swap" rel="stylesheet" />

    <style>
        :root {
            --navy: #0F172A;
            --navy-80: #1E293B;
            --indigo: #4F46E5;
            --indigo-bg: #EEF2FF;
            --cyan: #06B6D4;
            --cyan-bg: #ECFEFF;
            --amber: #F59E0B;
            --amber-bg: #FFFBEB;
            --slate-100: #F1F5F9;
            --slate-200: #E2E8F0;
            --slate-400: #94A3B8;
            --slate-500: #64748B;
            --slate-600: #475569;
            --font-display: 'Syne', sans-serif;
            --font-body: 'DM Sans', sans-serif;
            --radius-lg: 20px;
            --radius-md: 14px;
            --radius-sm: 9px;
            --shadow-sm: 0 1px 4px rgba(15,23,42,.06);
            --shadow-md: 0 4px 20px rgba(15,23,42,.09);
            --shadow-lg: 0 12px 48px rgba(15,23,42,.13);
        }

        *, *::before, *::after {
            box-sizing: border-box;
        }

        html, body {
            margin: 0;
            padding: 0;
            background: #F8FAFC;
            font-family: var(--font-body);
            color: var(--navy);
            min-height: 100vh;
            -webkit-font-smoothing: antialiased;
        }

        /* ─── TOPBAR ─── */
        .topbar {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            z-index: 100;
            height: 56px;
            background: rgba(255,255,255,.9);
            backdrop-filter: blur(12px);
            -webkit-backdrop-filter: blur(12px);
            border-bottom: 1px solid var(--slate-200);
            display: flex;
            align-items: center;
            padding: 0 24px;
            gap: 16px;
        }

        .topbar-brand {
            display: flex;
            align-items: center;
            gap: 9px;
            font-family: var(--font-display);
            font-size: 15px;
            font-weight: 700;
            color: var(--navy);
            text-decoration: none;
        }

        .topbar-brand-icon {
            width: 30px;
            height: 30px;
            background: linear-gradient(135deg,var(--indigo),var(--cyan));
            border-radius: 8px;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 14px;
        }

        .topbar-badge {
            padding: 3px 10px;
            border-radius: 99px;
            font-size: 11px;
            font-weight: 600;
            background: linear-gradient(135deg,var(--indigo-bg),var(--cyan-bg));
            color: var(--indigo);
            border: 1px solid #c7d2fe;
            margin-left: 4px;
        }

        .topbar-right {
            margin-left: auto;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .topbar-link {
            font-size: 13px;
            color: var(--slate-600);
            text-decoration: none;
            padding: 5px 12px;
            border-radius: var(--radius-sm);
            transition: background .15s;
        }

            .topbar-link:hover {
                background: var(--slate-100);
                color: var(--navy);
            }

        .topbar-btn {
            padding: 7px 16px;
            border-radius: var(--radius-sm);
            background: var(--indigo);
            color: white;
            font-size: 13px;
            font-weight: 600;
            border: none;
            cursor: pointer;
            transition: all .15s;
            display: flex;
            align-items: center;
            gap: 6px;
        }

            .topbar-btn:hover {
                background: #4338ca;
                box-shadow: 0 4px 14px rgba(79,70,229,.3);
            }

        /* ─── PAGE SHELL ─── */
        .page-shell {
            padding-top: 56px;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }

        /* ─── HERO ─── */
        .hero {
            position: relative;
            padding: 56px 24px 44px;
            text-align: center;
            overflow: hidden;
        }

        .hero-horizon {
            position: absolute;
            bottom: 0;
            left: 50%;
            transform: translateX(-50%);
            width: 600px;
            height: 2px;
            background: linear-gradient(90deg,transparent 0%,var(--indigo) 25%,var(--cyan) 50%,var(--amber) 75%,transparent 100%);
            border-radius: 99px;
            animation: horizon-shimmer 4s ease-in-out infinite;
        }

        @@keyframes horizon-shimmer {
            0%, 100% {
                opacity: .6;
                width: 400px;
            }

            50% {
                opacity: 1;
                width: 680px;
            }
        }

        .hero-glow {
            position: absolute;
            top: -60px;
            left: 50%;
            transform: translateX(-50%);
            width: 500px;
            height: 300px;
            background: radial-gradient(ellipse at center,rgba(79,70,229,.08) 0%,rgba(6,182,212,.05) 50%,transparent 80%);
            pointer-events: none;
        }

        .hero-eyebrow {
            display: inline-flex;
            align-items: center;
            gap: 7px;
            padding: 6px 16px;
            border-radius: 99px;
            background: white;
            border: 1px solid var(--slate-200);
            box-shadow: var(--shadow-sm);
            font-size: 12px;
            font-weight: 600;
            color: var(--slate-600);
            margin-bottom: 18px;
        }

        .hero-eyebrow-dot {
            width: 7px;
            height: 7px;
            border-radius: 50%;
            background: linear-gradient(135deg,var(--indigo),var(--cyan));
            animation: pulse-glow 2s ease-in-out infinite;
            flex-shrink: 0;
        }

        @@keyframes pulse-glow {
            0%, 100% {
                box-shadow: 0 0 0 0 rgba(79,70,229,.4);
            }

            50% {
                box-shadow: 0 0 0 5px rgba(79,70,229,.0);
            }
        }

        .hero-title {
            font-family: var(--font-display);
            font-size: clamp(28px,5vw,48px);
            font-weight: 800;
            color: var(--navy);
            letter-spacing: -.03em;
            line-height: 1.1;
            margin-bottom: 12px;
        }

        .hero-title-accent {
            background: linear-gradient(135deg,var(--indigo) 0%,var(--cyan) 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .hero-sub {
            font-size: 15px;
            line-height: 1.65;
            color: var(--slate-500);
            font-weight: 300;
            max-width: 500px;
            margin: 0 auto 24px;
        }

        .chip-row {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            gap: 8px;
            max-width: 620px;
            margin: 0 auto;
        }

        .chip {
            display: inline-flex;
            align-items: center;
            gap: 6px;
            padding: 7px 15px;
            border-radius: 99px;
            background: white;
            border: 1.5px solid var(--slate-200);
            font-size: 13px;
            font-weight: 500;
            color: var(--slate-600);
            cursor: pointer;
            transition: all .15s;
            box-shadow: var(--shadow-sm);
            user-select: none;
        }

            .chip:hover {
                border-color: var(--indigo);
                background: var(--indigo-bg);
                color: var(--indigo);
                transform: translateY(-1px);
                box-shadow: 0 4px 12px rgba(79,70,229,.15);
            }

        /* ─── MAIN LAYOUT ─── */
        .main-content {
            flex: 1;
            max-width: 1200px;
            margin: 0 auto;
            width: 100%;
            padding: 0 24px 60px;
            display: grid;
            grid-template-columns: 1fr 300px;
            gap: 24px;
            align-items: start;
        }

        .main-left {
            display: flex;
            flex-direction: column;
            gap: 18px;
        }

        /* ─── PROMPT CARD ─── */
        .prompt-card {
            background: white;
            border: 1.5px solid var(--slate-200);
            border-radius: var(--radius-lg);
            box-shadow: var(--shadow-md);
            padding: 20px;
            transition: border-color .2s,box-shadow .2s;
        }

            .prompt-card:focus-within {
                border-color: var(--indigo);
                box-shadow: 0 0 0 4px rgba(79,70,229,.08),var(--shadow-md);
            }

        .prompt-card-label {
            display: flex;
            align-items: center;
            gap: 8px;
            font-size: 11px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: .8px;
            color: var(--slate-400);
            margin-bottom: 10px;
        }

        .prompt-card-label-dot {
            width: 6px;
            height: 6px;
            border-radius: 50%;
            background: linear-gradient(135deg,var(--indigo),var(--cyan));
            flex-shrink: 0;
        }

        .prompt-textarea {
            width: 100%;
            border: none;
            outline: none;
            resize: none;
            font-family: var(--font-body);
            font-size: 15px;
            line-height: 1.6;
            color: var(--navy);
            background: transparent;
            min-height: 90px;
            padding: 0;
        }

            .prompt-textarea::placeholder {
                color: var(--slate-400);
                font-weight: 300;
            }

        .prompt-examples {
            display: flex;
            flex-wrap: wrap;
            gap: 6px;
            padding: 10px 0;
            border-top: 1px solid var(--slate-100);
            border-bottom: 1px solid var(--slate-100);
            margin: 10px 0;
            align-items: center;
        }

        .prompt-examples-label {
            font-size: 10px;
            font-weight: 700;
            color: var(--slate-400);
            text-transform: uppercase;
            letter-spacing: .6px;
            margin-right: 4px;
            white-space: nowrap;
        }

        .prompt-example {
            display: inline-flex;
            align-items: center;
            gap: 5px;
            padding: 4px 11px;
            border-radius: 6px;
            background: var(--slate-100);
            font-size: 11.5px;
            color: var(--slate-500);
            cursor: pointer;
            transition: all .13s;
            font-style: italic;
        }

            .prompt-example:hover {
                background: var(--indigo-bg);
                color: var(--indigo);
            }

        .prompt-footer {
            display: flex;
            align-items: center;
            gap: 8px;
            padding-top: 10px;
        }

        .prompt-icon-btn {
            width: 36px;
            height: 36px;
            border-radius: var(--radius-sm);
            border: 1.5px solid var(--slate-200);
            background: white;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 16px;
            color: var(--slate-500);
            cursor: pointer;
            transition: all .13s;
            flex-shrink: 0;
        }

            .prompt-icon-btn:hover {
                border-color: var(--indigo);
                color: var(--indigo);
                background: var(--indigo-bg);
            }

        .prompt-footer-hint {
            font-size: 11.5px;
            color: var(--slate-400);
            margin-left: auto;
            margin-right: 8px;
        }

        .prompt-send-btn {
            display: flex;
            align-items: center;
            gap: 7px;
            padding: 9px 22px;
            border-radius: var(--radius-sm);
            background: linear-gradient(135deg,var(--indigo) 0%,#6366f1 100%);
            color: white;
            border: none;
            cursor: pointer;
            font-family: var(--font-body);
            font-size: 14px;
            font-weight: 600;
            transition: all .15s;
            box-shadow: 0 4px 14px rgba(79,70,229,.3);
            flex-shrink: 0;
        }

            .prompt-send-btn:hover {
                background: linear-gradient(135deg,#4338ca,#4f46e5);
                box-shadow: 0 6px 20px rgba(79,70,229,.4);
                transform: translateY(-1px);
            }

            .prompt-send-btn:active {
                transform: translateY(0);
            }

            .prompt-send-btn:disabled {
                opacity: .65;
                cursor: not-allowed;
                transform: none;
                box-shadow: none;
            }

            .prompt-send-btn .btn-spinner {
                display: none;
            }

            .prompt-send-btn.loading .bi-send-fill {
                display: none;
            }

            .prompt-send-btn.loading .btn-spinner {
                display: inline-block;
            }

        /* ─── QUICK CARDS ─── */
        .quick-cards {
            display: grid;
            grid-template-columns: repeat(4,1fr);
            gap: 12px;
        }

        .quick-card {
            background: white;
            border: 1.5px solid var(--slate-200);
            border-radius: var(--radius-md);
            padding: 16px 14px;
            cursor: pointer;
            transition: all .18s;
            display: flex;
            flex-direction: column;
            gap: 10px;
            box-shadow: var(--shadow-sm);
            position: relative;
            overflow: hidden;
        }

            .quick-card::before {
                content: '';
                position: absolute;
                bottom: 0;
                left: 0;
                right: 0;
                height: 2px;
                border-radius: 0 0 var(--radius-md) var(--radius-md);
                opacity: 0;
                transition: opacity .18s;
            }

            .quick-card:hover {
                transform: translateY(-2px);
                box-shadow: var(--shadow-md);
                border-color: transparent;
            }

                .quick-card:hover::before {
                    opacity: 1;
                }

        .qc-food::before {
            background: linear-gradient(90deg,var(--amber),#fb923c);
        }

        .qc-attraction::before {
            background: linear-gradient(90deg,var(--indigo),var(--cyan));
        }

        .qc-hotel::before {
            background: linear-gradient(90deg,#8b5cf6,var(--indigo));
        }

        .qc-transport::before {
            background: linear-gradient(90deg,var(--cyan),#10b981);
        }

        .qc-food:hover {
            background: #fffdf8;
        }

        .qc-attraction:hover {
            background: var(--indigo-bg);
        }

        .qc-hotel:hover {
            background: #faf5ff;
        }

        .qc-transport:hover {
            background: var(--cyan-bg);
        }

        .quick-card-icon {
            width: 38px;
            height: 38px;
            border-radius: 10px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 18px;
        }

        .qc-food .quick-card-icon {
            background: var(--amber-bg);
            color: var(--amber);
        }

        .qc-attraction .quick-card-icon {
            background: var(--indigo-bg);
            color: var(--indigo);
        }

        .qc-hotel .quick-card-icon {
            background: #f5f3ff;
            color: #8b5cf6;
        }

        .qc-transport .quick-card-icon {
            background: var(--cyan-bg);
            color: var(--cyan);
        }

        .quick-card-title {
            font-family: var(--font-display);
            font-size: 13px;
            font-weight: 700;
            color: var(--navy);
            margin-bottom: 2px;
        }

        .quick-card-desc {
            font-size: 11.5px;
            color: var(--slate-500);
            line-height: 1.45;
        }

        .quick-card-arrow {
            margin-top: auto;
            font-size: 13px;
            color: var(--slate-400);
            transition: color .15s,transform .15s;
        }

        .quick-card:hover .quick-card-arrow {
            color: var(--indigo);
            transform: translateX(3px);
        }

        /* ─── AGENT ACTIVITY ─── */
        .agent-activity {
            background: white;
            border: 1.5px solid var(--slate-200);
            border-radius: var(--radius-md);
            box-shadow: var(--shadow-sm);
            overflow: hidden;
            display: none;
        }

            .agent-activity.visible {
                display: block;
                animation: fadeIn .25s ease;
            }

        .activity-head {
            padding: 11px 16px;
            border-bottom: 1px solid var(--slate-100);
            display: flex;
            align-items: center;
            gap: 8px;
            background: #fafbfd;
        }

        .activity-head-ico {
            width: 26px;
            height: 26px;
            border-radius: 7px;
            background: linear-gradient(135deg,var(--indigo),var(--cyan));
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 11px;
        }

        .activity-head-title {
            font-size: 12px;
            font-weight: 700;
            color: var(--navy);
        }

        .activity-head-badge {
            margin-left: auto;
            padding: 2px 9px;
            border-radius: 99px;
            font-size: 10.5px;
            font-weight: 600;
            background: var(--indigo-bg);
            color: var(--indigo);
            border: 1px solid #c7d2fe;
        }

        .activity-steps {
            padding: 12px 16px;
            display: flex;
            flex-direction: column;
            gap: 6px;
        }

        .activity-step {
            display: flex;
            align-items: center;
            gap: 8px;
            font-size: 12px;
            color: var(--slate-500);
            transition: color .2s;
        }

        .activity-step-ico {
            width: 18px;
            height: 18px;
            border-radius: 50%;
            border: 1.5px solid var(--slate-300);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 9px;
            color: var(--slate-400);
            flex-shrink: 0;
            transition: all .2s;
        }

        .activity-step.done .activity-step-ico {
            background: #f0fdf4;
            border-color: #bbf7d0;
            color: #16a34a;
        }

        .activity-step.done {
            color: var(--navy);
        }

        .activity-step.active .activity-step-ico {
            background: var(--indigo-bg);
            border-color: #c7d2fe;
            color: var(--indigo);
            animation: spin .8s linear infinite;
        }

        .activity-step.active {
            color: var(--indigo);
            font-weight: 600;
        }

        @@keyframes spin {
            to {
                transform: rotate(360deg);
            }
        }

        /* ─── CONVERSATION AREA ─── */
        .convo-area {
            background: white;
            border: 1.5px solid var(--slate-200);
            border-radius: var(--radius-lg);
            box-shadow: var(--shadow-sm);
            min-height: 360px;
            display: flex;
            flex-direction: column;
            overflow: hidden;
        }

        .convo-header {
            padding: 13px 18px;
            border-bottom: 1px solid var(--slate-100);
            display: flex;
            align-items: center;
            gap: 10px;
            background: #fafbfd;
            flex-shrink: 0;
        }

        .convo-header-ico {
            width: 30px;
            height: 30px;
            border-radius: 8px;
            background: linear-gradient(135deg,var(--indigo),var(--cyan));
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 13px;
        }

        .convo-header-title {
            font-size: 13.5px;
            font-weight: 700;
            color: var(--navy);
        }

        .convo-header-sub {
            font-size: 11px;
            color: var(--slate-400);
        }

        .convo-header-badge {
            margin-left: auto;
            padding: 3px 9px;
            border-radius: 99px;
            background: #f0fdf4;
            color: #16a34a;
            font-size: 11px;
            font-weight: 600;
            border: 1px solid #bbf7d0;
            display: flex;
            align-items: center;
            gap: 4px;
        }

            .convo-header-badge::before {
                content: '';
                width: 5px;
                height: 5px;
                border-radius: 50%;
                background: #16a34a;
                flex-shrink: 0;
                animation: pulse-green 2s ease-in-out infinite;
            }

        @@keyframes pulse-green {
            0%, 100% {
                opacity: 1;
            }

            50% {
                opacity: .3;
            }
        }

        /* Empty state */
        .convo-empty {
            flex: 1;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            padding: 36px 24px;
            text-align: center;
        }

        .convo-empty-globe {
            width: 76px;
            height: 76px;
            border-radius: 50%;
            background: linear-gradient(135deg,var(--indigo-bg) 0%,var(--cyan-bg) 100%);
            border: 2px dashed var(--slate-200);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 34px;
            margin-bottom: 16px;
            position: relative;
        }

            .convo-empty-globe::after {
                content: '✈️';
                position: absolute;
                top: -8px;
                right: -8px;
                width: 28px;
                height: 28px;
                border-radius: 50%;
                background: white;
                border: 1.5px solid var(--slate-200);
                display: flex;
                align-items: center;
                justify-content: center;
                font-size: 14px;
            }

        .convo-empty-title {
            font-family: var(--font-display);
            font-size: 16px;
            font-weight: 700;
            color: var(--navy);
            margin-bottom: 6px;
        }

        .convo-empty-sub {
            font-size: 13px;
            color: var(--slate-400);
            max-width: 260px;
            line-height: 1.6;
            margin-bottom: 20px;
        }

        .convo-empty-prompts {
            display: flex;
            flex-direction: column;
            gap: 7px;
            width: 100%;
            max-width: 340px;
        }

        .cep {
            padding: 10px 14px;
            background: var(--slate-100);
            border-radius: var(--radius-sm);
            font-size: 12.5px;
            color: var(--slate-600);
            cursor: pointer;
            transition: all .13s;
            text-align: left;
            border: 1px solid transparent;
            display: flex;
            align-items: center;
            gap: 8px;
        }

            .cep i {
                color: var(--indigo);
                font-size: 13px;
                flex-shrink: 0;
            }

            .cep:hover {
                background: var(--indigo-bg);
                border-color: #c7d2fe;
                color: var(--indigo);
            }

        /* Messages */
        .convo-messages {
            flex: 1;
            padding: 16px 18px;
            display: flex;
            flex-direction: column;
            gap: 16px;
            overflow-y: auto;
            max-height: 500px;
        }

        .msg-user, .msg-ai {
            display: flex;
            gap: 10px;
            align-items: flex-start;
        }

        .msg-user {
            flex-direction: row-reverse;
        }

        .msg-avatar {
            width: 32px;
            height: 32px;
            border-radius: 50%;
            flex-shrink: 0;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 14px;
        }

        .msg-ai .msg-avatar {
            background: linear-gradient(135deg,var(--indigo),var(--cyan));
            color: white;
        }

        .msg-user .msg-avatar {
            background: var(--slate-200);
            color: var(--slate-600);
        }

        .msg-bubble {
            max-width: 82%;
            padding: 11px 15px;
            border-radius: 14px;
            font-size: 13.5px;
            line-height: 1.65;
        }

        .msg-user .msg-bubble {
            background: var(--indigo);
            color: white;
            border-radius: 14px 4px 14px 14px;
        }

        .msg-ai .msg-bubble {
            background: white;
            color: var(--navy);
            border: 1.5px solid var(--slate-200);
            border-radius: 4px 14px 14px 14px;
            box-shadow: var(--shadow-sm);
        }
            /* Markdown render */
            .msg-ai .msg-bubble p {
                margin: 0 0 8px;
            }

                .msg-ai .msg-bubble p:last-child {
                    margin-bottom: 0;
                }

            .msg-ai .msg-bubble ul,
            .msg-ai .msg-bubble ol {
                padding-left: 20px;
                margin: 8px 0;
            }

            .msg-ai .msg-bubble li {
                margin-bottom: 4px;
            }

            .msg-ai .msg-bubble strong {
                font-weight: 700;
                color: var(--navy-80);
            }

            .msg-ai .msg-bubble hr {
                border: none;
                border-top: 1px solid var(--slate-200);
                margin: 10px 0;
            }

            /* YENİ: hata balonu varyantı */
            .msg-ai .msg-bubble.msg-error {
                background: #fef2f2;
                border-color: #fecaca;
                color: #b91c1c;
            }

        .msg-ai-meta {
            font-size: 10.5px;
            color: var(--slate-400);
            margin-top: 5px;
            padding-left: 2px;
        }

        /* Typing indicator */
        .msg-typing {
            display: flex;
            align-items: center;
            gap: 5px;
            padding: 12px 15px;
            background: white;
            border: 1.5px solid var(--slate-200);
            border-radius: 4px 14px 14px 14px;
            box-shadow: var(--shadow-sm);
        }

        .typing-text {
            font-size: 12px;
            color: var(--slate-400);
            margin-left: 2px;
            font-style: italic;
        }

        .typing-dot {
            width: 6px;
            height: 6px;
            border-radius: 50%;
            background: var(--indigo);
            animation: typing-bounce .8s ease-in-out infinite;
        }

            .typing-dot:nth-child(2) {
                animation-delay: .15s;
            }

            .typing-dot:nth-child(3) {
                animation-delay: .3s;
            }

        @@keyframes typing-bounce {
            0%, 80%, 100% {
                transform: translateY(0);
            }

            40% {
                transform: translateY(-5px);
            }
        }

        /* ─── RIGHT PANEL ─── */
        .right-panel {
            display: flex;
            flex-direction: column;
            gap: 14px;
            position: sticky;
            top: 72px;
        }

        .glass-card {
            background: rgba(255,255,255,.88);
            backdrop-filter: blur(12px);
            -webkit-backdrop-filter: blur(12px);
            border: 1.5px solid rgba(255,255,255,.9);
            border-radius: var(--radius-md);
            box-shadow: var(--shadow-md);
            overflow: hidden;
        }

        .panel-head {
            padding: 12px 16px 10px;
            border-bottom: 1px solid var(--slate-100);
            display: flex;
            align-items: center;
            gap: 8px;
            background: rgba(248,250,252,.85);
        }

        .panel-head-ico {
            width: 28px;
            height: 28px;
            border-radius: 7px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 13px;
        }

        .panel-head-title {
            font-size: 12.5px;
            font-weight: 700;
            color: var(--navy);
        }

        .panel-head-sub {
            font-size: 10.5px;
            color: var(--slate-400);
        }

        .panel-body {
            padding: 14px 16px;
        }

        .panel-empty-body {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            text-align: center;
            padding: 20px 16px;
            min-height: 96px;
        }

        .panel-empty-ico {
            font-size: 30px;
            margin-bottom: 9px;
            opacity: .55;
        }

        .panel-empty-title {
            font-size: 13px;
            font-weight: 700;
            color: var(--slate-600);
            margin-bottom: 4px;
        }

        .panel-empty-desc {
            font-size: 11.5px;
            color: var(--slate-400);
            line-height: 1.5;
        }

        /* ═══════════════════════════════════════════
                           YENİ: HAVA DURUMU KARTI İÇERİK STİLLERİ
                           (mevcut palet ve radius değişkenleri kullanıldı)
                        ═══════════════════════════════════════════ */
        .weather-city {
            font-family: var(--font-display);
            font-size: 15px;
            font-weight: 700;
            color: var(--navy);
            line-height: 1.25;
        }

        .weather-country {
            font-size: 11px;
            color: var(--slate-400);
            margin-bottom: 10px;
        }

        .weather-main {
            display: flex;
            align-items: center;
            gap: 12px;
            padding-bottom: 12px;
            border-bottom: 1px solid var(--slate-100);
        }

        .weather-main-icon {
            font-size: 34px;
            line-height: 1;
            flex-shrink: 0;
        }

        .weather-temp {
            font-family: var(--font-display);
            font-size: 30px;
            font-weight: 800;
            color: var(--navy);
            letter-spacing: -.02em;
            line-height: 1;
        }

        .weather-condition {
            font-size: 12px;
            color: var(--slate-500);
            margin-top: 3px;
        }

        .weather-details {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 8px 10px;
            padding: 12px 0;
            border-bottom: 1px solid var(--slate-100);
        }

        .weather-detail {
            display: flex;
            align-items: center;
            gap: 6px;
            font-size: 11.5px;
            color: var(--slate-600);
            min-width: 0;
        }

            .weather-detail i {
                color: var(--cyan);
                font-size: 12px;
                flex-shrink: 0;
            }

            .weather-detail span {
                overflow: hidden;
                text-overflow: ellipsis;
                white-space: nowrap;
            }

        .weather-sun {
            display: flex;
            justify-content: space-between;
            gap: 8px;
            padding: 11px 0 2px;
        }

        .weather-sun-item {
            display: flex;
            align-items: center;
            gap: 6px;
            font-size: 11.5px;
            color: var(--slate-600);
        }

            .weather-sun-item i.bi-sunrise-fill {
                color: var(--amber);
            }

            .weather-sun-item i.bi-sunset-fill {
                color: #fb923c;
            }

        /* Günlük tahmin kartları — mobilde yatay kaydırılabilir */
        .weather-forecast-wrap {
            border-top: 1px solid var(--slate-100);
            padding-top: 11px;
            margin-top: 10px;
        }

        .weather-forecast-label {
            font-size: 10px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: .6px;
            color: var(--slate-400);
            margin-bottom: 8px;
        }

        .weather-forecast {
            display: flex;
            gap: 6px;
            overflow-x: auto;
            padding-bottom: 4px;
            scrollbar-width: thin;
            -webkit-overflow-scrolling: touch;
        }

            .weather-forecast::-webkit-scrollbar {
                height: 4px;
            }

            .weather-forecast::-webkit-scrollbar-thumb {
                background: var(--slate-200);
                border-radius: 99px;
            }

        .forecast-card {
            flex: 1 0 56px;
            min-width: 56px;
            background: white;
            border: 1px solid var(--slate-200);
            border-radius: var(--radius-sm);
            padding: 8px 4px;
            text-align: center;
            box-shadow: var(--shadow-sm);
        }

        .forecast-day {
            font-size: 10.5px;
            font-weight: 700;
            color: var(--navy);
            margin-bottom: 3px;
        }

        .forecast-icon {
            font-size: 17px;
            line-height: 1;
            margin-bottom: 3px;
        }

        .forecast-temp {
            font-size: 11px;
            font-weight: 600;
            color: var(--slate-600);
            white-space: nowrap;
        }

        .forecast-cond {
            font-size: 9px;
            color: var(--slate-400);
            line-height: 1.25;
            margin-top: 2px;
        }
        /* ═══════ /YENİ HAVA DURUMU STİLLERİ ═══════ */

        /* ─── FOOTER ─── */
        .page-footer {
            border-top: 1px solid var(--slate-200);
            padding: 13px 24px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            font-size: 12px;
            color: var(--slate-400);
            flex-wrap: wrap;
            gap: 8px;
            background: white;
        }

        .footer-left {
            display: flex;
            align-items: center;
            gap: 6px;
        }

        .footer-link {
            color: var(--slate-400);
            text-decoration: none;
        }

            .footer-link:hover {
                color: var(--indigo);
            }

        .footer-powered {
            display: flex;
            align-items: center;
            gap: 6px;
            padding: 5px 12px;
            border-radius: 99px;
            background: linear-gradient(135deg,var(--indigo-bg),var(--cyan-bg));
            color: var(--indigo);
            font-size: 11.5px;
            font-weight: 600;
            border: 1px solid #c7d2fe;
        }

        /* ─── ANIMATIONS ─── */
        .fade-in {
            animation: fadeIn .35s ease forwards;
        }

        @@keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(8px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .slide-up {
            animation: slideUp .4s ease forwards;
        }

        @@keyframes slideUp {
            from {
                opacity: 0;
                transform: translateY(16px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* ─── TOAST ─── */
        .toast-bar {
            position: fixed;
            bottom: 24px;
            right: 24px;
            z-index: 9999;
            background: var(--navy);
            color: white;
            padding: 12px 18px;
            border-radius: var(--radius-sm);
            font-size: 13px;
            font-weight: 500;
            box-shadow: var(--shadow-lg);
            opacity: 0;
            transform: translateY(8px);
            transition: all .25s;
            pointer-events: none;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        /* ─── RESPONSIVE ─── */
        @@media (max-width:1024px) {
            .main-content {
                grid-template-columns: 1fr;
            }

            .right-panel {
                position: static;
                display: grid;
                grid-template-columns: repeat(2,1fr);
            }
        }

        @@media (max-width:768px) {
            .main-content {
                padding: 0 16px 48px;
            }

            .hero {
                padding: 36px 16px 32px;
            }

            .quick-cards {
                grid-template-columns: repeat(2,1fr);
            }

            .right-panel {
                grid-template-columns: 1fr;
            }

            .topbar-link {
                display: none;
            }

            .hero-title {
                font-size: 26px;
            }
            /* YENİ: mobilde tahmin kartları sabit genişlik + yatay kaydırma */
            .forecast-card {
                flex: 0 0 62px;
                min-width: 62px;
            }
        }

        @@media (max-width:480px) {
            .quick-cards {
                grid-template-columns: 1fr 1fr;
            }

            .chip {
                font-size: 12px;
                padding: 5px 12px;
            }
            /* YENİ: dar ekranda detay grid'i tek sütun */
            .weather-details {
                grid-template-columns: 1fr;
            }
        }

        @@media (prefers-reduced-motion:reduce) {
            .hero-horizon, .hero-eyebrow-dot, .typing-dot, .fade-in, .slide-up {
                animation: none !important;
            }
        }
    </style>
</head>
<body>

    <!-- TOPBAR -->
    <nav class="topbar">
        <a href="/" class="topbar-brand">
            <div class="topbar-brand-icon"><i class="bi bi-airplane-fill"></i></div>
            FlightBooking
            <span class="topbar-badge"><i class="bi bi-stars me-1"></i>AI</span>
        </a>
        <a href="/Admin" class="topbar-link"><i class="bi bi-grid me-1"></i>Kontrol Paneli</a>
        <a href="/Flights" class="topbar-link"><i class="bi bi-airplane me-1"></i>Uçuşlar</a>
        <a href="/Bookings" class="topbar-link"><i class="bi bi-calendar-check me-1"></i>Rezervasyonlar</a>
        <div class="topbar-right">
            <a href="#" class="topbar-link"><i class="bi bi-bell"></i></a>
            <button class="topbar-btn"><i class="bi bi-person-circle"></i> Hesabım</button>
        </div>
    </nav>

    <div class="page-shell">

        <!-- HERO -->
        <section class="hero">
            <div class="hero-glow"></div>
            <div class="hero-eyebrow">
                <span class="hero-eyebrow-dot"></span>
                GPT-4o destekli · Gerçek zamanlı yanıtlar
            </div>
            <h1 class="hero-title">
                <span class="hero-title-accent">Yapay Zeka</span><br>Seyahat Asistanı
            </h1>
            <p class="hero-sub">
                Gideceğiniz şehir, restoranlar, gezilecek yerler, ulaşım ve seyahat planlaması hakkında bana istediğiniz soruyu sorabilirsiniz.
            </p>
            <div class="chip-row">
                <span class="chip" onclick="insertChip('Restoran bul:')">🍔 Restoran Bul</span>
                <span class="chip" onclick="insertChip('Gezilecek yerler:')">🏛️ Gezilecek Yerler</span>
                <span class="chip" onclick="insertChip('Seyahat planla:')">✈️ Seyahat Planla</span>
                <span class="chip" onclick="insertChip('Ulaşım seçenekleri:')">🚇 Ulaşım</span>
                <span class="chip" onclick="insertChip('Yerel kafeler:')">☕ Yerel Kafeler</span>
                <span class="chip" onclick="insertChip('Aile aktiviteleri:')">👨‍👩‍👧 Aile Aktiviteleri</span>
            </div>
            <div class="hero-horizon"></div>
        </section>

        <!-- MAIN -->
        <div class="main-content">

            <!-- Sol kolon -->
            <div class="main-left">

                <!-- Prompt Kartı -->
                <div class="prompt-card fade-in">
                    <div class="prompt-card-label">
                        <span class="prompt-card-label-dot"></span>
                        Seyahat sorunuzu sorun
                    </div>
                    <textarea id="promptInput"
                              asp-for="Prompt"
                              class="prompt-textarea"
                              rows="3"
                              placeholder="Seyahatiniz hakkında bir soru sorun..."
                              onkeydown="handleKeydown(event)"></textarea>
                    <div class="prompt-examples">
                        <span class="prompt-examples-label">Örnek:</span>
                        <!-- DEĞİŞTİ: hava durumu örneği eklendi (Weather intent'i test etmek için) -->
                        <span class="prompt-example" onclick="setPrompt('Trabzon\'da hava durumu nasıl?')">
                            <i class="bi bi-lightning-fill"></i>Trabzon hava durumu
                        </span>
                        <span class="prompt-example" onclick="setPrompt('Bursa\'da çocuklu aile için en iyi hamburgerci mekanları hangileri?')">
                            <i class="bi bi-lightning-fill"></i>Bursa'da çocuklu hamburgerci
                        </span>
                        <span class="prompt-example" onclick="setPrompt('Roma\'da 3 günlük gezi planı hazırla, mutlaka görülmesi gereken yerleri ekle')">
                            <i class="bi bi-lightning-fill"></i>Roma'da 3 günlük plan
                        </span>
                        <span class="prompt-example" onclick="setPrompt('İstanbul\'dan Milano\'ya havalimanı ulaşım seçenekleri neler?')">
                            <i class="bi bi-lightning-fill"></i>İstanbul–Milano ulaşım
                        </span>
                    </div>
                    <div class="prompt-footer">
                        <button type="button" class="prompt-icon-btn" title="Dosya ekle"
                                onclick="showToast('Dosya ekleme yakında aktif olacak')">
                            <i class="bi bi-paperclip"></i>
                        </button>
                        <button type="button" class="prompt-icon-btn" title="Konum ekle"
                                onclick="showToast('Konum ekleme yakında aktif olacak')">
                            <i class="bi bi-geo-alt-fill"></i>
                        </button>
                        <button type="button" class="prompt-icon-btn" title="Sesli giriş"
                                onclick="showToast('Sesli giriş yakında aktif olacak')">
                            <i class="bi bi-mic-fill"></i>
                        </button>
                        <span class="prompt-footer-hint" id="charCount">0 / 500</span>
                        <button type="button" class="prompt-send-btn" id="sendBtn" onclick="sendMessage()">
                            <span class="btn-spinner spinner-border spinner-border-sm" role="status"
                                  style="width:14px;height:14px;border-width:2px;"></span>
                            <i class="bi bi-send-fill"></i>
                            Sor
                        </button>
                    </div>
                </div>

                <!-- Hızlı Kartlar -->
                <div class="quick-cards fade-in" style="animation-delay:.08s;">
                    <div class="quick-card qc-food" onclick="insertChip('Restoran bul:')">
                        <div class="quick-card-icon"><i class="bi bi-cup-hot-fill"></i></div>
                        <div>
                            <div class="quick-card-title">Restoranlar</div>
                            <div class="quick-card-desc">Yerel lezzetlerden fine dining'e her bütçeye</div>
                        </div>
                        <div class="quick-card-arrow"><i class="bi bi-arrow-right"></i></div>
                    </div>
                    <div class="quick-card qc-attraction" onclick="insertChip('Gezilecek yerler:')">
                        <div class="quick-card-icon"><i class="bi bi-binoculars-fill"></i></div>
                        <div>
                            <div class="quick-card-title">Gezilecek Yerler</div>
                            <div class="quick-card-desc">Müzeler, tarihi mekanlar, gizli köşeler</div>
                        </div>
                        <div class="quick-card-arrow"><i class="bi bi-arrow-right"></i></div>
                    </div>
                    <div class="quick-card qc-hotel" onclick="insertChip('En iyi oteller:')">
                        <div class="quick-card-icon"><i class="bi bi-building-fill"></i></div>
                        <div>
                            <div class="quick-card-title">Oteller</div>
                            <div class="quick-card-desc">Her bütçeye uygun konaklama seçenekleri</div>
                        </div>
                        <div class="quick-card-arrow"><i class="bi bi-arrow-right"></i></div>
                    </div>
                    <div class="quick-card qc-transport" onclick="insertChip('Ulaşım seçenekleri:')">
                        <div class="quick-card-icon"><i class="bi bi-train-front-fill"></i></div>
                        <div>
                            <div class="quick-card-title">Ulaşım</div>
                            <div class="quick-card-desc">Metro, otobüs, taksi ve havalimanı bağlantıları</div>
                        </div>
                        <div class="quick-card-arrow"><i class="bi bi-arrow-right"></i></div>
                    </div>
                </div>

                <!-- Agent Activity -->
                <div class="agent-activity" id="agentActivity">
                    <div class="activity-head">
                        <div class="activity-head-ico"><i class="bi bi-cpu-fill"></i></div>
                        <div>
                            <div class="activity-head-title" style="font-size:12px;font-weight:700;color:var(--navy);">Agent Activity</div>
                        </div>
                        <div class="activity-head-badge" id="activityBadge">İşleniyor</div>
                    </div>
                    <div class="activity-steps" id="activitySteps">
                        <div class="activity-step" id="step-1">
                            <div class="activity-step-ico"><i class="bi bi-check-lg"></i></div>
                            <span>Prompt Alındı</span>
                        </div>
                        <div class="activity-step" id="step-2">
                            <div class="activity-step-ico"><i class="bi bi-check-lg"></i></div>
                            <span>İstek Hazırlanıyor</span>
                        </div>
                        <div class="activity-step" id="step-3">
                            <div class="activity-step-ico"><i class="bi bi-check-lg"></i></div>
                            <span>OpenAI Bağlanıyor</span>
                        </div>
                        <div class="activity-step" id="step-4">
                            <div class="activity-step-ico"><i class="bi bi-check-lg"></i></div>
                            <span>Yanıt Bekleniyor</span>
                        </div>
                        <div class="activity-step" id="step-5">
                            <div class="activity-step-ico"><i class="bi bi-check-lg"></i></div>
                            <span>Yanıt Alındı</span>
                        </div>
                    </div>
                </div>

                <!-- Konuşma Alanı -->
                <div class="convo-area fade-in" id="convoArea" style="animation-delay:.14s;">
                    <div class="convo-header">
                        <div class="convo-header-ico"><i class="bi bi-robot"></i></div>
                        <div>
                            <div class="convo-header-title">Seyahat Asistanı</div>
                            <div class="convo-header-sub">Yolculuğunuz için kişiselleştirilmiş</div>
                        </div>
                        <div class="convo-header-badge">Çevrimiçi</div>
                    </div>

                    <!-- Boş durum -->
                    <div class="convo-empty" id="emptyState">
                        <div class="convo-empty-globe">🌍</div>
                        <div class="convo-empty-title">Henüz bir konuşma yok</div>
                        <div class="convo-empty-sub">İlk seyahat sorunuzu yazarak başlayın.</div>
                        <div class="convo-empty-prompts">
                            <!-- DEĞİŞTİ: hava durumu örnek promptu eklendi -->
                            <div class="cep" onclick="setPrompt('Trabzon\'da hava durumu nasıl?')">
                                <i class="bi bi-cloud-sun-fill"></i>
                                Trabzon'da hava durumu nasıl?
                            </div>
                            <div class="cep" onclick="setPrompt('Bursa\'da çocuklu aile için en iyi hamburgerci ve yemek mekanları hangileri?')">
                                <i class="bi bi-geo-alt-fill"></i>
                                Bursa'da çocuklu aile için hamburgerci öner
                            </div>
                            <div class="cep" onclick="setPrompt('Roma\'da 3 günlük gezi planı hazırla, mutlaka görülmesi gereken yerleri ekle')">
                                <i class="bi bi-calendar3"></i>
                                Roma'da 3 günlük gezi planı hazırla
                            </div>
                            <div class="cep" onclick="setPrompt('İstanbul\'dan Milano\'ya giden biri için havalimanı ulaşım seçenekleri neler?')">
                                <i class="bi bi-train-front-fill"></i>
                                İstanbul'dan Milano'ya havalimanı ulaşımı öner
                            </div>
                        </div>
                    </div>

                    <!-- Mesajlar -->
                    <div class="convo-messages" id="messages" style="display:none;"></div>
                </div>

            </div>

            <!-- Sağ Panel -->
            <div class="right-panel">

                <!-- ═══ DEĞİŞTİ: HAVA DURUMU KARTI (dinamik) ═══ -->
                <div class="glass-card slide-up" id="weatherCard" style="animation-delay:.1s;">
                    <div class="panel-head">
                        <div class="panel-head-ico" style="background:#ecfeff;color:#0891b2;"><i class="bi bi-cloud-sun-fill"></i></div>
                        <div>
                            <div class="panel-head-title">Hava Durumu</div>
                            <div class="panel-head-sub" id="weatherHeadSub">Gerçek zamanlı bilgi</div>
                        </div>
                    </div>

                    <!-- Empty state (varsayılan görünüm) -->
                    <div class="panel-body panel-empty-body" id="weatherEmpty">
                        <div class="panel-empty-ico">🌤️</div>
                        <div class="panel-empty-title">Henüz bir şehir seçilmedi</div>
                        <div class="panel-empty-desc">Bir destinasyon yazdığınızda hava durumu burada görünür.</div>
                    </div>

                    <!-- Gerçek veri alanı — JS ile doldurulur -->
                    <div class="panel-body" id="weatherContent" style="display:none;"></div>
                </div>
                <!-- ═══ /HAVA DURUMU KARTI ═══ -->
                <!-- Aşağıdaki kartlar değişmedi: empty state olarak kalıyor -->
                <div class="glass-card slide-up" style="animation-delay:.18s;">
                    <div class="panel-head">
                        <div class="panel-head-ico" style="background:var(--indigo-bg);color:var(--indigo);"><i class="bi bi-clock-fill"></i></div>
                        <div>
                            <div class="panel-head-title">Yerel Saat</div>
                            <div class="panel-head-sub">Dünya saatleri</div>
                        </div>
                    </div>
                    <div class="panel-body panel-empty-body">
                        <div class="panel-empty-ico">🕐</div>
                        <div class="panel-empty-title">Şehir bekleniyor</div>
                        <div class="panel-empty-desc">Destinasyon seçtikten sonra yerel saat bilgisi burada gösterilir.</div>
                    </div>
                </div>

                <div class="glass-card slide-up" style="animation-delay:.24s;">
                    <div class="panel-head">
                        <div class="panel-head-ico" style="background:#f0fdf4;color:#16a34a;"><i class="bi bi-currency-exchange"></i></div>
                        <div>
                            <div class="panel-head-title">Döviz</div>
                            <div class="panel-head-sub">Canlı kur bilgisi</div>
                        </div>
                    </div>
                    <div class="panel-body panel-empty-body">
                        <div class="panel-empty-ico">💱</div>
                        <div class="panel-empty-title">Destinasyon gerekli</div>
                        <div class="panel-empty-desc">Para birimi bilgisi için gideceğiniz şehri belirtin.</div>
                    </div>
                </div>

                <div class="glass-card slide-up" style="animation-delay:.30s;">
                    <div class="panel-head">
                        <div class="panel-head-ico" style="background:var(--amber-bg);color:var(--amber);"><i class="bi bi-fire"></i></div>
                        <div>
                            <div class="panel-head-title">Popüler Yerler</div>
                            <div class="panel-head-sub">Önerilen destinasyonlar</div>
                        </div>
                    </div>
                    <div class="panel-body panel-empty-body">
                        <div class="panel-empty-ico">🗺️</div>
                        <div class="panel-empty-title">Öneri yok henüz</div>
                        <div class="panel-empty-desc">Öneriler için önce bir şehir veya rota belirtin.</div>
                    </div>
                </div>
            </div>

        </div>

        <!-- FOOTER -->
        <footer class="page-footer">
            <div class="footer-left">
                <i class="bi bi-airplane-fill" style="color:var(--indigo);"></i>
                FlightBooking YZ Seyahat Asistanı
                <span style="color:var(--slate-200);">·</span>
                <a href="#" class="footer-link">Gizlilik</a>
                <span style="color:var(--slate-200);">·</span>
                <a href="#" class="footer-link">Şartlar</a>
            </div>
            <div class="footer-powered">
                <i class="bi bi-stars"></i>
                OpenAI GPT-4o destekli
            </div>
        </footer>

    </div><!-- /page-shell -->
    <!-- Toast -->
    <div class="toast-bar" id="toast">
        <i class="bi bi-info-circle-fill" style="color:var(--cyan);"></i>
        <span id="toastMsg"></span>
    </div>

    <script>
        /* ════════════════════════════════════════════
           YARDIMCI FONKSİYONLAR
        ════════════════════════════════════════════ */
        const promptInput = document.getElementById('promptInput');
        const charCount   = document.getElementById('charCount');
        const sendBtn     = document.getElementById('sendBtn');
        const messagesEl  = document.getElementById('messages');
        const emptyEl     = document.getElementById('emptyState');

        /* YENİ: hava durumu kartı elemanları */
        const weatherEmptyEl   = document.getElementById('weatherEmpty');
        const weatherContentEl = document.getElementById('weatherContent');
        const weatherHeadSubEl = document.getElementById('weatherHeadSub');

        let isLoading = false;

        /* Karakter sayacı */
        promptInput.addEventListener('input', function () {
            const len = this.value.length;
            charCount.textContent = len + ' / 500';
            charCount.style.color = len > 450 ? '#dc2626' : 'var(--slate-400)';
            if (len > 500) this.value = this.value.substring(0, 500);
        });

        function insertChip(text) {
            promptInput.value = text + ' ';
            promptInput.focus();
            promptInput.dispatchEvent(new Event('input'));
        }

        function setPrompt(text) {
            promptInput.value = text.replace(/^"/, '').replace(/"$/, '').trim();
            promptInput.focus();
            promptInput.dispatchEvent(new Event('input'));
        }

        function handleKeydown(e) {
            if (e.key === 'Enter' && (e.ctrlKey || e.metaKey)) {
                e.preventDefault();
                sendMessage();
            }
        }

        /* Toast */
        let toastTimer;
        function showToast(msg) {
            const t = document.getElementById('toast');
            document.getElementById('toastMsg').textContent = msg;
            t.style.opacity = '1'; t.style.transform = 'translateY(0)';
            clearTimeout(toastTimer);
            toastTimer = setTimeout(() => {
                t.style.opacity = '0'; t.style.transform = 'translateY(8px)';
            }, 2800);
        }

        /* YENİ: XSS'e karşı basit HTML escape (hava durumu alanları için) */
        function escapeHtml(value) {
            if (value === null || value === undefined) return '';
            return String(value)
                .replace(/&/g, '&amp;')
                .replace(/</g, '&lt;')
                .replace(/>/g, '&gt;')
                .replace(/"/g, '&quot;')
                .replace(/'/g, '&#39;');
        }

        /* ════════════════════════════════════════════
           AGENT ACTIVITY
        ════════════════════════════════════════════ */
        const steps = ['step-1','step-2','step-3','step-4','step-5'];
        let stepTimers = [];

        function showAgentActivity() {
            const panel = document.getElementById('agentActivity');
            panel.classList.add('visible');
            /* Reset */
            steps.forEach(id => {
                const el = document.getElementById(id);
                el.classList.remove('done','active');
            });
            document.getElementById('activityBadge').textContent = 'İşleniyor';

            /* Kademeli ilerleme */
            const delays = [0, 300, 700, 1200, 0]; /* son adım addAIMessage'da tetiklenir */
            steps.forEach((id, i) => {
                if (i === steps.length - 1) return; /* son adım manuel */
                const t = setTimeout(() => {
                    /* Öncekini done yap */
                    if (i > 0) document.getElementById(steps[i-1]).classList.replace('active','done');
                    document.getElementById(id).classList.add('active');
                }, delays[i]);
                stepTimers.push(t);
            });
        }

        function completeAgentActivity() {
            stepTimers.forEach(clearTimeout); stepTimers = [];
            steps.forEach(id => {
                const el = document.getElementById(id);
                el.classList.remove('active');
                el.classList.add('done');
            });
            document.getElementById('activityBadge').textContent = 'Tamamlandı';
            document.getElementById('activityBadge').style.background = '#f0fdf4';
            document.getElementById('activityBadge').style.color = '#16a34a';
            document.getElementById('activityBadge').style.borderColor = '#bbf7d0';
        }

        function resetAgentActivity() {
            stepTimers.forEach(clearTimeout); stepTimers = [];
            const panel = document.getElementById('agentActivity');
            panel.classList.remove('visible');
            const badge = document.getElementById('activityBadge');
            badge.textContent = 'İşleniyor';
            badge.style.background = ''; badge.style.color = ''; badge.style.borderColor = '';
        }

        /* ════════════════════════════════════════════
           KONUŞMA FONKSİYONLARI
        ════════════════════════════════════════════ */
        function addUserMessage(text) {
            const div = document.createElement('div');
            div.className = 'msg-user fade-in';
            div.innerHTML = `
                <div class="msg-avatar"><i class="bi bi-person-fill"></i></div>
                <div class="msg-bubble">${text.replace(/</g,'&lt;').replace(/>/g,'&gt;')}</div>`;
            messagesEl.appendChild(div);
            messagesEl.scrollTop = messagesEl.scrollHeight;
        }

        function showTypingIndicator() {
            const div = document.createElement('div');
            div.className = 'msg-ai fade-in';
            div.id = 'typingIndicator';
            div.innerHTML = `
                <div class="msg-avatar"><i class="bi bi-robot"></i></div>
                <div class="msg-typing">
                    <div class="typing-dot"></div>
                    <div class="typing-dot"></div>
                    <div class="typing-dot"></div>
                    <span class="typing-text">YZ düşünüyor...</span>
                </div>`;
            messagesEl.appendChild(div);
            messagesEl.scrollTop = messagesEl.scrollHeight;
        }

        function removeTypingIndicator() {
            const el = document.getElementById('typingIndicator');
            if (el) el.remove();
        }

        function renderMarkdown(text) {
            /* Satır temizliği */
            let html = text
                /* Başlıklar */
                .replace(/^### (.+)$/gm, '<h5 style="font-family:var(--font-display);font-size:13px;font-weight:700;margin:10px 0 4px">$1</h5>')
                .replace(/^## (.+)$/gm,  '<h4 style="font-family:var(--font-display);font-size:14px;font-weight:700;margin:12px 0 5px">$1</h4>')
                .replace(/^# (.+)$/gm,   '<h3 style="font-family:var(--font-display);font-size:15px;font-weight:800;margin:14px 0 6px">$1</h3>')
                /* Kalın */
                .replace(/\*\*(.*?)\*\*/g, '<strong>$1</strong>')
                /* İtalik */
                .replace(/\*(.*?)\*/g, '<em>$1</em>')
                /* Yatay çizgi */
                .replace(/^---$/gm, '<hr style="border:none;border-top:1px solid var(--slate-200);margin:10px 0;">')
                /* Bullet list */
                .replace(/^[•\-\*] (.+)$/gm, '<li>$1</li>')
                /* Numaralı list */
                .replace(/^\d+\. (.+)$/gm, '<li>$1</li>');

            /* li'leri ul'e sar */
            html = html.replace(/(<li>.*?<\/li>\n?)+/gs, (match) => `<ul style="padding-left:18px;margin:6px 0;">${match}</ul>`);

            /* Paragraf */
            const lines = html.split('\n');
            const result = [];
            for (let i = 0; i < lines.length; i++) {
                const line = lines[i].trim();
                if (!line) { result.push(''); continue; }
                if (line.startsWith('<h') || line.startsWith('<ul') || line.startsWith('<hr')) {
                    result.push(line);
                } else {
                    result.push(`<p style="margin:0 0 6px;">${line}</p>`);
                }
            }
            return result.join('');
        }

        /* DEĞİŞTİ: isError parametresi eklendi (hata balonu için) */
        function addAIMessage(text, meta, isError) {
            const now = new Date().toLocaleTimeString('tr-TR', { hour:'2-digit', minute:'2-digit' });
            const div = document.createElement('div');
            div.className = 'msg-ai fade-in';
            const modelInfo = meta && meta.model ? ` · ${meta.model}` : '';
            const errClass  = isError ? ' msg-error' : '';
            const bubbleHtml = isError
                ? `<i class="bi bi-exclamation-triangle-fill me-1"></i>${escapeHtml(text)}`
                : renderMarkdown(text);
            div.innerHTML = `
                <div class="msg-avatar"><i class="bi bi-robot"></i></div>
                <div>
                    <div class="msg-bubble${errClass}">${bubbleHtml}</div>
                    <div class="msg-ai-meta">
                        <i class="bi bi-stars me-1"></i>YZ Seyahat Asistanı · ${now}${modelInfo}
                    </div>
                </div>`;
            messagesEl.appendChild(div);
            messagesEl.scrollTop = messagesEl.scrollHeight;
        }

        /* ════════════════════════════════════════════
           YENİ: HAVA DURUMU FONKSİYONLARI
        ════════════════════════════════════════════ */

        /* condition metnine göre emoji seç */
        function getWeatherIcon(condition) {
            if (!condition) return '🌡️';
            const c = String(condition).toLowerCase();

            if (c.includes('thunder') || c.includes('storm')) return '⛈️';
            if (c.includes('snow') || c.includes('sleet') || c.includes('flurr')) return '❄️';
            if (c.includes('rain') || c.includes('shower') || c.includes('drizzle')) return '🌧️';
            if (c.includes('partly cloudy') || c.includes('partly sunny')) return '🌤️';
            if (c.includes('cloud') || c.includes('overcast')) return '☁️';
            if (c.includes('sunny') || c.includes('clear') || c.includes('fair')) return '☀️';
            return '🌡️';
        }

        /* "5:05 AM" → "05:05" biçimine çevir; parse edilemezse orijinali döndür */
        function formatClock(value) {
            if (!value) return '-';
            const raw = String(value).trim();
            const m = raw.match(/^(\d{1,2}):(\d{2})\s*(AM|PM)?$/i);
            if (!m) return raw;
            let h = parseInt(m[1], 10);
            const min = m[2];
            const suffix = m[3] ? m[3].toUpperCase() : null;
            if (suffix === 'PM' && h < 12) h += 12;
            if (suffix === 'AM' && h === 12) h = 0;
            return String(h).padStart(2, '0') + ':' + min;
        }

        /* Boş/undefined değerler için güvenli gösterim */
        function val(v, suffix) {
            if (v === null || v === undefined || v === '') return '-';
            return escapeHtml(v) + (suffix || '');
        }

        /* Hava durumu kartını gerçek veriyle doldur */
        function updateWeatherPanel(weather) {
            if (!weather) return;

            /* camelCase bekleniyor; PascalCase gelirse de çalışsın diye fallback */
            const city        = weather.city        ?? weather.City;
            const country     = weather.country     ?? weather.Country;
            const temperature = weather.temperature ?? weather.Temperature;
            const condition   = weather.condition   ?? weather.Condition;
            const humidity    = weather.humidity    ?? weather.Humidity;
            const windSpeed   = weather.windSpeed   ?? weather.WindSpeed;
            const windDir     = weather.windDirection ?? weather.WindDirection;
            const visibility  = weather.visibility  ?? weather.Visibility;
            const pressure    = weather.pressure    ?? weather.Pressure;
            const sunrise     = weather.sunrise     ?? weather.Sunrise;
            const sunset      = weather.sunset      ?? weather.Sunset;
            const forecasts   = weather.forecasts   ?? weather.Forecasts ?? [];

            /* Ana blok */
            let html = `
                <div class="weather-city">${val(city)}</div>
                <div class="weather-country">${val(country)}</div>

                <div class="weather-main">
                    <div class="weather-main-icon">${getWeatherIcon(condition)}</div>
                    <div>
                        <div class="weather-temp">${temperature ?? '-'}°C</div>
                        <div class="weather-condition">${val(condition)}</div>
                    </div>
                </div>

                <div class="weather-details">
                    <div class="weather-detail" title="Nem">
                        <i class="bi bi-droplet-fill"></i><span>Nem: %${val(humidity)}</span>
                    </div>
                    <div class="weather-detail" title="Rüzgar">
                        <i class="bi bi-wind"></i><span>${val(windSpeed)} km/sa</span>
                    </div>
                    <div class="weather-detail" title="Rüzgar yönü">
                        <i class="bi bi-compass-fill"></i><span>${val(windDir)}</span>
                    </div>
                    <div class="weather-detail" title="Görüş mesafesi">
                        <i class="bi bi-eye-fill"></i><span>Görüş: ${val(visibility)} km</span>
                    </div>
                    <div class="weather-detail" title="Basınç">
                        <i class="bi bi-speedometer2"></i><span>${val(pressure)} hPa</span>
                    </div>
                </div>

                <div class="weather-sun">
                    <div class="weather-sun-item">
                        <i class="bi bi-sunrise-fill"></i>${formatClock(sunrise)}
                    </div>
                    <div class="weather-sun-item">
                        <i class="bi bi-sunset-fill"></i>${formatClock(sunset)}
                    </div>
                </div>`;

            /* Günlük tahminler — ilk 5 gün */
            if (Array.isArray(forecasts) && forecasts.length > 0) {
                const list = forecasts.slice(0, 5);
                html += `
                <div class="weather-forecast-wrap">
                    <div class="weather-forecast-label">Günlük Tahmin</div>
                    <div class="weather-forecast">`;
                list.forEach(f => {
                    const day  = f.day       ?? f.Day;
                    const low  = f.low       ?? f.Low;
                    const high = f.high      ?? f.High;
                    const cond = f.condition ?? f.Condition;
                    html += `
                        <div class="forecast-card">
                            <div class="forecast-day">${val(day)}</div>
                            <div class="forecast-icon">${getWeatherIcon(cond)}</div>
                            <div class="forecast-temp">${low ?? '-'}° / ${high ?? '-'}°</div>
                            <div class="forecast-cond">${val(cond)}</div>
                        </div>`;
                });
                html += `
                    </div>
                </div>`;
            }

            /* DOM'a yaz: empty state'i gizle, içeriği göster */
            weatherContentEl.innerHTML = html;
            weatherEmptyEl.style.display = 'none';
            weatherContentEl.style.display = 'block';
            weatherContentEl.classList.remove('fade-in');
            void weatherContentEl.offsetWidth; /* animasyonu yeniden tetikle */
            weatherContentEl.classList.add('fade-in');

            if (weatherHeadSubEl) {
                weatherHeadSubEl.textContent = 'Güncellendi · ' +
                    new Date().toLocaleTimeString('tr-TR', { hour: '2-digit', minute: '2-digit' });
            }
        }

        /* Kartı ilk haline (empty state) döndür */
        function resetWeatherPanel() {
            weatherContentEl.innerHTML = '';
            weatherContentEl.style.display = 'none';
            weatherEmptyEl.style.display = 'flex';
            if (weatherHeadSubEl) weatherHeadSubEl.textContent = 'Gerçek zamanlı bilgi';
        }
        /* ═══════ /YENİ HAVA DURUMU FONKSİYONLARI ═══════ */

        /* ════════════════════════════════════════════
           sendMessage()  —  gerçek API çağrısı
        ════════════════════════════════════════════ */
        async function sendMessage() {
            const text = promptInput.value.trim();
            if (!text || isLoading) return;

            /* Boş durumu gizle, mesajları göster */
            emptyEl.style.display = 'none';
            messagesEl.style.display = 'flex';

            /* UI kilitle */
            isLoading = true;
            sendBtn.classList.add('loading');
            sendBtn.disabled = true;

            /* NOT: Hava durumu kartına burada dokunulmuyor.
               Eski veri varsa yeni cevap gelene kadar ekranda kalır. */

            /* Kullanıcı mesajını ekle */
            addUserMessage(text);
            promptInput.value = '';
            promptInput.dispatchEvent(new Event('input'));

            /* Agent activity başlat */
            showAgentActivity();

            /* Typing indicator */
            showTypingIndicator();

            try {
                const response = await fetch('/Agent/AskAgent', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                        'RequestVerificationToken': document.querySelector('input[name="__RequestVerificationToken"]')?.value || ''
                    },
                    body: JSON.stringify({ prompt: text })
                });

                /* DEĞİŞTİ: JSON parse hatasına karşı korumalı okuma */
                let data = null;
                try {
                    data = await response.json();
                } catch (parseErr) {
                    console.error('JSON parse hatası:', parseErr);
                }

                removeTypingIndicator();
                completeAgentActivity();

                /* DEĞİŞTİ: spec'e uygun hata kontrolü */
                if (!response.ok || !data || !data.isSuccess) {
                    console.error('Agent API başarısız:', {
                        status: response.status,
                        statusText: response.statusText,
                        payload: data
                    });
                    addAIMessage('Şu anda hava durumu bilgisi alınamadı. Lütfen tekrar deneyin.', null, true);
                    return;
                }

                /* AI cevabı */
                addAIMessage(data.response || 'Yanıt boş döndü.', { model: data.model });

                /* YENİ: Weather intent ise sağ paneli güncelle */
                if (data.intent === 'Weather' && data.weather) {
                    updateWeatherPanel(data.weather);
                }

            } catch (err) {
                removeTypingIndicator();
                completeAgentActivity();
                addAIMessage('Şu anda hava durumu bilgisi alınamadı. Lütfen tekrar deneyin.', null, true);
                console.error('Agent API hatası:', err);
            } finally {
                /* UI aç */
                isLoading = false;
                sendBtn.classList.remove('loading');
                sendBtn.disabled = false;
                promptInput.focus();
                /* Activity paneli 3sn sonra gizle */
                setTimeout(resetAgentActivity, 3000);
            }
        }
    </script>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>